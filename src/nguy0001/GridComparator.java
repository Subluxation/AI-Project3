package nguy0001;

import java.util.Comparator;

public class GridComparator implements Comparator<GridSquare> {

	@Override
	public int compare(GridSquare o1, GridSquare o2) {
		return (o1).CompareTo(o2);
	}
}